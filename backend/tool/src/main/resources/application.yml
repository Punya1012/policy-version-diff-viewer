spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tooldb
    username: postgres
    password: word
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  security:
    user:
      name: admin
      password: admin

server:
  port: 8080